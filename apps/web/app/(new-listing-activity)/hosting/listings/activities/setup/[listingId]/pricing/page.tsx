import { HOST } from "@/common/constants"
import { APP_NAME } from "@repo/constants"
import { Metadata } from "next"
import AuthGuard from "@/common/components/AuthGuard"
import ActivityPricing from "@/module/Hosting/Listings/Activities/Activity/Pricing"

export const metadata: Metadata = {
  title: `${HOST} - ${APP_NAME}`,
  description: `Generated by ${APP_NAME}`,
}

const ActivityPricingPage = () => {
  return (
    <AuthGuard>
      <ActivityPricing pageType="setup" />
    </AuthGuard>
  )
}

export default ActivityPricingPage
