import { Typography } from "@/common/components/ui/Typography";

const NewlyAddedTag = () => {
  return (
    <Typography variant="h5" className="px-2 text-text-500 font-semibold rounded-xl min-w-24 bg-white shadow absolute left-3 top-3 z-40">
      Newly added
    </Typography>
  );
}

export default NewlyAddedTag